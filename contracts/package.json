{
  "name": "monadpay-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for monadpay payment application",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:monad": "hardhat run scripts/deploy.js --network monad",
    "deploy:testnet": "hardhat run scripts/deploy.js --network monadTestnet",
    "verify": "hardhat verify --network",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "ethers": "^6.9.0"
  }
}


